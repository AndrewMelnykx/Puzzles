let numNodes = 10;
let parentArray = [9, 7, 5, 5, 2, 9, 9, 9, 2, -1];

var findTreeHeight = function (numNodes, parentArray) {
  // Your implementation here
};

let output = findTreeHeight(numNodes, parentArray);

console.log(output);
