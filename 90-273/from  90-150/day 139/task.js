let string = "loveleetcode";
let c = "e";

var shortestToChar = function (s, c) {
  let resArr = [];
  for (let i = 0; i < s.length; i++) {
    if (s[i]) {
    }
  }
};

let output = shortestToChar(string, c);

console.log(output);
